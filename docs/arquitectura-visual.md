# 🏗️ Arquitectura del Sistema VERTEX

## Diagrama de Capas (Clean Architecture)

```
┌────────────────────────────────────────────────────────────────┐
│                         VERTEX BACKEND                          │
│                        Clean Architecture                       │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                     CAPA 4: PRESENTACIÓN                        │
│                        (Vertex.API)                             │
├────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │ AuthController   │  │ OnboardingController │                   │
│  │  - POST /register│  │  - POST /save    │                    │
│  │  - POST /login   │  │  - GET /resume   │                    │
│  │  + JWT Generator │  │  + [Authorize]   │                    │
│  └──────────────────┘  └──────────────────┘                    │
│  ┌──────────────────────────────────────────┐                  │
│  │  Program.cs                               │                  │
│  │  - ASP.NET Core Identity                  │                  │
│  │  - JWT Bearer Authentication              │                  │
│  │  - DI Container                           │                  │
│  │  - Middleware Pipeline                    │                  │
│  └──────────────────────────────────────────┘                  │
│              ↓                    ↓                             │
└─────────────────────────┬──────────────────────────────────────┘
                          │
                          │ Inyección de Dependencias
                          ↓
┌────────────────────────────────────────────────────────────────┐
│              CAPA 3: INFRAESTRUCTURA                            │
│                 (Vertex.Infrastructure)                         │
├────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐      │
│  │  VertexDbContext : IdentityDbContext<ApplicationUser>│      │
│  │  - DbSet<OnboardingProcess>                          │      │
│  │  - DbSet<ProfessionalProfile>                        │      │
│  │  - Identity Tables (AspNetUsers, AspNetRoles, etc.)  │      │
│  │  - Configuraciones (OnModelCreating)                 │      │
│  └──────────────────────────────────────────────────────┘      │
│                            ↓                                    │
│  ┌──────────────────────────────────────────────────────┐      │
│  │          OnboardingRepository                        │      │
│  │  - GetByUserIdAsync()                                │      │
│  │  - SaveOrUpdateAsync() ← Lógica de Upsert           │      │
│  └──────────────────────────────────────────────────────┘      │
│                            ↓                                    │
│              ┌────────────────────────┐                         │
│              │      SQL Server        │                         │
│              │    (VertexDB)          │                         │
│              └────────────────────────┘                         │
└─────────────────────────┬──────────────────────────────────────┘
                          │
                          │ Implementa Interfaces
                          ↓
┌────────────────────────────────────────────────────────────────┐
│                CAPA 2: APLICACIÓN                               │
│                 (Vertex.Application)                            │
├────────────────────────────────────────────────────────────────┤
│  ┌────────────────────┐         ┌──────────────────────┐       │
│  │    INTERFACES      │         │    ONBOARDING DTOs   │       │
│  ├────────────────────┤         ├──────────────────────┤       │
│  │ IOnboardingRepository │         │ SaveProgressDto      │       │
│  │ - GetByUserIdAsync()│         │ - CurrentStep        │       │
│  │ - SaveOrUpdateAsync()│         │ - SerializedData     │       │
│  └────────────────────┘         │ - IsCompleted        │       │
│                                  │ (UserId eliminado)   │       │
│                                  └──────────────────────┘       │
│                                  ┌──────────────────────┐       │
│                                  │ OnboardingStatusDto  │       │
│                                  │ - CurrentStep        │       │
│                                  │ - SerializedData     │       │
│                                  │ - IsCompleted        │       │
│                                  │ - UpdatedAt          │       │
│                                  └──────────────────────┘       │
│                                                                 │
│                                  ┌──────────────────────┐       │
│                                  │    AUTH DTOs         │       │
│                                  ├──────────────────────┤       │
│                                  │ RegisterDto          │       │
│                                  │ LoginDto             │       │
│                                  │ AuthResponseDto      │       │
│                                  └──────────────────────┘       │
└─────────────────────────┬──────────────────────────────────────┘
                          │
                          │ Usa
                          ↓
┌────────────────────────────────────────────────────────────────┐
│                 CAPA 1: DOMINIO (NÚCLEO)                        │
│                     (Vertex.Domain)                             │
├────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────┐  ┌──────────────────────────┐  │
│  │ ApplicationUser            │  │  OnboardingProcess       │  │
│  │ : IdentityUser             │  ├──────────────────────────┤  │
│  ├────────────────────────────┤  │ - Id (Guid)              │  │
│  │ - Id (string)              │  │ - UserId (string)        │  │
│  │ - Email (string)           │  │ - CurrentStep (int)      │  │
│  │ - PasswordHash (string)    │  │ - SerializedData (string)│  │
│  │ - FullName (string)        │  │ - IsCompleted (bool)     │  │
│  └────────────────────────────┘  │ - UpdatedAt (DateTime)   │  │
│                                  └──────────────────────────┘  │
│  ┌──────────────────────────┐                                  │
│  │  ProfessionalProfile     │                                  │
│  ├──────────────────────────┤                                  │
│  │ - Id (Guid)              │                                  │
│  │ - UserId (string)        │                                  │
│  │ - FullName (string)      │                                  │
│  │ - Summary (string)       │                                  │
│  │ - SkillsJson (string)    │                                  │
│  │ - CreatedAt (DateTime)   │                                  │
│  │ - UpdatedAt (DateTime)   │                                  │
│  └──────────────────────────┘                                  │
│                                                                 │
│         ⚡ ENTIDADES DE DOMINIO + IDENTITY USER                 │
└────────────────────────────────────────────────────────────────┘
```

---

## Flujo de Datos: Guardar Progreso

```
┌─────────────┐
│   Frontend  │ (React/Angular/Vue)
└──────┬──────┘
       │ HTTP POST /api/Onboarding/save
       │ Body: SaveProgressDto
       ↓
┌────────────────────────────────────────┐
│  OnboardingController                  │
│  1. Valida UserId                      │
│  2. Mapea DTO → Entidad                │
└──────┬─────────────────────────────────┘
       │ Llama al repositorio
       ↓
┌────────────────────────────────────────┐
│  OnboardingRepository                  │
│  3. GetByUserIdAsync()                 │
│     ¿Existe registro?                  │
│       ├─ SÍ → UPDATE                   │
│       └─ NO → INSERT                   │
└──────┬─────────────────────────────────┘
       │ EF Core ejecuta SQL
       ↓
┌────────────────────────────────────────┐
│  SQL Server (VertexDB)                 │
│  4. Tabla: OnboardingProcesses         │
│     - Guarda/actualiza registro        │
└──────┬─────────────────────────────────┘
       │ Retorna entidad guardada
       ↓
┌────────────────────────────────────────┐
│  OnboardingController                  │
│  5. Mapea Entidad → OnboardingStatusDto│
└──────┬─────────────────────────────────┘
       │ HTTP 200 OK
       ↓
┌─────────────┐
│   Frontend  │ Recibe estado actualizado
└─────────────┘
```

---

## Flujo de Datos: Recuperar Estado

```
┌─────────────┐
│   Frontend  │
└──────┬──────┘
       │ HTTP GET /api/Onboarding/resume?userId=xxx
       ↓
┌────────────────────────────────────────┐
│  OnboardingController                  │
│  1. Valida UserId                      │
└──────┬─────────────────────────────────┘
       │ Llama al repositorio
       ↓
┌────────────────────────────────────────┐
│  OnboardingRepository                  │
│  2. GetByUserIdAsync(userId)           │
└──────┬─────────────────────────────────┘
       │ Consulta EF Core
       ↓
┌────────────────────────────────────────┐
│  SQL Server (VertexDB)                 │
│  3. SELECT * FROM OnboardingProcesses  │
│     WHERE UserId = @userId             │
└──────┬─────────────────────────────────┘
       │ Retorna entidad o null
       ↓
┌────────────────────────────────────────┐
│  OnboardingController                  │
│  4. ¿Existe?                           │
│     ├─ SÍ → Mapea a DTO → 200 OK       │
│     └─ NO → 404 Not Found              │
└──────┬─────────────────────────────────┘
       │ HTTP Response
       ↓
┌─────────────┐
│   Frontend  │
└─────────────┘
```

---

## Reglas de Dependencia

```
    API
     ↓
Infrastructure ←─── Application
     ↓                   ↓
     └──────→ Domain ←───┘
```

### Principios:
1. **Domain** no conoce a nadie (núcleo puro)
2. **Application** solo conoce a Domain (contratos)
3. **Infrastructure** implementa contratos de Application y usa Domain
4. **API** orquesta todo usando Application e Infrastructure

---

## Tecnologías por Capa

| Capa | Tecnologías |
|------|-------------|
| **API** | ASP.NET Core 9, Swagger/OpenAPI, CORS |
| **Infrastructure** | EF Core 10, SQL Server, Identity Framework |
| **Application** | Interfaces (.NET), DTOs |
| **Domain** | C# POCOs (sin dependencias) |

---

## Ventajas de Clean Architecture

✅ **Testabilidad**: Cada capa se puede probar independientemente  
✅ **Mantenibilidad**: Cambios en una capa no afectan a las demás  
✅ **Independencia de Framework**: El dominio no depende de EF Core  
✅ **Independencia de Base de Datos**: Se puede cambiar SQL Server por PostgreSQL  
✅ **Escalabilidad**: Fácil agregar nuevas funcionalidades  
✅ **Separación de Responsabilidades**: Cada proyecto tiene un propósito claro  

---

## Patrón Repository

```
┌───────────────────────────────────────────┐
│         IOnboardingRepository             │ (Interfaz en Application)
│  + GetByUserIdAsync(userId)               │
│  + SaveOrUpdateAsync(process)             │
└─────────────────┬─────────────────────────┘
                  │
                  │ Implementa
                  ↓
┌───────────────────────────────────────────┐
│       OnboardingRepository                │ (Implementación en Infrastructure)
│  - _context: VertexDbContext              │
│  + GetByUserIdAsync(userId)               │
│    └─> _context.OnboardingProcesses       │
│        .FirstOrDefaultAsync()             │
│  + SaveOrUpdateAsync(process)             │
│    ├─> Verifica existencia                │
│    ├─> UPDATE si existe                   │
│    └─> INSERT si no existe                │
└───────────────────────────────────────────┘
```

---

## Configuración del DbContext

```csharp
// VertexDbContext hereda de IdentityDbContext
IdentityDbContext
    ↓
VertexDbContext
    ├─> DbSet<OnboardingProcess>
    ├─> DbSet<ProfessionalProfile>
    └─> OnModelCreating()
        ├─> Configurar índice único en UserId
        ├─> Configurar nvarchar(max) para JSON
        └─> Configurar valores por defecto
```

---

## Inyección de Dependencias (DI)

```csharp
// En Program.cs (Vertex.API)

builder.Services.AddDbContext<VertexDbContext>(options =>
    options.UseSqlServer(connectionString)
);

builder.Services.AddScoped<IOnboardingRepository, OnboardingRepository>();
                   ↑                              ↑
                Contrato                   Implementación
           (Application)                 (Infrastructure)
```

Cuando un controlador requiere `IOnboardingRepository`, el contenedor de DI automáticamente inyecta `OnboardingRepository`.

---

Última actualización: Enero 22, 2026
